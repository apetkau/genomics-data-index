name: gdi
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - bedtools
  - pyqt
  - iqtree
  - python=3.8
  - bcftools
  - htslib
  - ete3
  - pip
  - pip:
    - genomics-data-index
prefix: /home/apetkau/miniconda3/envs/gdi
